---
const { href } = Astro.props
---

<a
  class="social-pill rounded-full border border-white/10 flex justify-center items-center gap-x-2 py-1 px-4 md:py-2 md:px-4 text-xs md:text-sm transition bg-white/5 hover:scale-110 hover:bg-white/10"
  href={href}
  target="_blank"
  rel="noopener noreferrer"
>
  <slot />
</a>

<style>
  .social-pill {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 100%;
    /* Add consistent min-width to ensure pills have similar size */
    min-width: 6rem;
    /* Fixed height for consistency */
    height: 2rem;
    /* Center content vertically */
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  /* Apply responsive adjustments */
  @media (min-width: 768px) {
    .social-pill {
      min-width: 7rem;
      height: 2.5rem;
    }
  }
</style>

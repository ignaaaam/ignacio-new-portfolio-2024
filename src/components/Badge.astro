---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span class:list={["inline-flex items-center text-sm font-medium px-3 py-1.5 rounded-md", className]}>
  <slot />
</span>
